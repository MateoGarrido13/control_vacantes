# Isolated local Postgres (docker compose --profile local-db). Ignores SUPABASE_DB_URL.
spring.datasource.url=jdbc:postgresql://localhost:5433/vacantes?sslmode=disable
spring.datasource.username=vacantes
spring.datasource.password=vacantes
spring.jpa.show-sql=true
