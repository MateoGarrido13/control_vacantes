#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

usage() {
  echo "uso: run vacantes" >&2
  echo "     ./run vacantes" >&2
  exit 1
}

[[ "${1:-}" == "vacantes" ]] || usage

exec "$ROOT/scripts/run-vacantes.sh"
